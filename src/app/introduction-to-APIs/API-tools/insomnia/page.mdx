import BackToTop from "@/components/BackToTop";
import Button from "@/components/Button";
import { FaChevronRight } from "react-icons/fa";

# Insomnia

## Table of Contents

## Introduction

[Insomnia](https://insomnia.rest/) is a API development platform that streamlines the entire API lifecycle from design to testing and collaboration. Originally created as a REST client, Insomnia has evolved into a full-featured API development ecosystem that supports modern development workflows with powerful debugging, mocking, testing, and collaboration capabilities.

Insomnia stands out for its intuitive interface that caters to both technical and non-technical team members, making API development accessible across different roles within an organization. It seamlessly integrates design-first and code-first approaches, allowing teams to work with OpenAPI specifications, generate live documentation, and maintain consistency across development stages.

The platform supports multiple protocols including REST, GraphQL, SOAP, gRPC, WebSockets, and Server-Sent Events, making it a versatile solution for modern microservices architectures. With robust collaboration features, enterprise-grade security, and native Git integration, Insomnia enables teams to scale API development efficiently while maintaining quality and consistency.

![Insomnia Interface](https://insomnia.rest/images/home/hero-graphic-insomnia-rest-20241210.png)

##### CAUTION

> Pricing information is current as of June 2025 and subject to change. Many tools offer free tiers or trial periods.

### Key Features

| Feature                        | Description                                                                                |
| ------------------------------ | ------------------------------------------------------------------------------------------ |
| **Multi-Protocol Support**     | Comprehensive support for REST, GraphQL, SOAP, gRPC, WebSockets, and Server-Sent Events.   |
| **Live OpenAPI Preview**       | Real-time visualization and interaction with OpenAPI specifications.                       |
| **API Design & Spec Editor**   | Built-in editor for creating and managing OpenAPI specifications with live validation.     |
| **Automated Test Generation**  | Generate collections and tests automatically from OpenAPI specs.                           |
| **Insomnia CLI (inso)**        | Powerful command-line interface for automating API testing and CI/CD integration.          |
| **Mock Servers**               | Create and deploy mock servers locally or in the cloud to simulate API responses.          |
| **Advanced Authentication**    | Support for OAuth 2.0, SAML, OIDC, API keys, Basic Auth, and enterprise SSO methods.       |
| **Multi-Format Support**       | Handle JSON, XML, YAML, HTML, and CORS for diverse API requirements.                       |
| **Cloud Collaboration**        | Real-time team collaboration with cloud hosting and project sharing capabilities.          |
| **Native Git Integration**     | Version control with native Git support for tracking API changes and collaboration.        |
| **JavaScript Testing**         | Write and execute JavaScript-based tests with pre/post-request scripting capabilities.     |
| **Request Chaining**           | Create complex workflows by chaining requests and using response data in subsequent calls. |
| **Environment Management**     | Manage multiple environments (dev, staging, prod) with variable inheritance and scoping.   |
| **Import/Export Capabilities** | Import from Postman, Swagger, HAR files, and export to various formats.                    |
| **Code Generation**            | Generate client SDKs and code snippets in 20+ programming languages.                       |
| **Themes & Customization**     | Extensive theming options and plugin ecosystem for customization.                          |
| **Enterprise Security**        | End-to-end encryption, RBAC, domain control, and vault integrations for enterprise use.    |
| **Performance Monitoring**     | Built-in performance tracking and analytics for API health monitoring.                     |
| **Scratch Pad**                | Quick testing environment for ad-hoc API exploration without saving to collections.        |
| **Collection Runner**          | Automated execution of test suites with reporting and CI/CD integration.                   |

<BackToTop />

## Getting Started

Insomnia is available as a cross-platform desktop application for Windows, macOS, and Linux, as well as a web application for browser-based access. The platform offers multiple deployment options to fit different team workflows and security requirements.

### Download Options

| Platform                                                                    | Description                                      | Best For                                                   |
| --------------------------------------------------------------------------- | ------------------------------------------------ | ---------------------------------------------------------- |
| **[Desktop App](https://insomnia.rest/download)**                           | Native desktop application with full feature set | Individual developers, offline work, local development     |
| **[Web App](https://app.insomnia.rest/)**                                   | Browser-based access with core features          | Quick access, team collaboration, no installation required |
| **[Insomnia CLI (inso)](https://docs.insomnia.rest/inso-cli/introduction)** | Command-line interface for automation            | CI/CD pipelines, automated testing, DevOps workflows       |

### System Requirements

**Desktop Application**:

- Windows: Windows 10 or later (64-bit)
- macOS: macOS 10.14 or later
- Linux: Ubuntu 16.04+, Fedora 24+, or equivalent

**Web Application**:

- Modern web browser (Chrome 80+, Firefox 75+, Safari 13+, Edge 80+)
- Internet connection for cloud features

### Installation Process

1. **Visit** the [Insomnia download page](https://insomnia.rest/download)
2. **Select** your operating system
3. **Download** the appropriate installer
4. **Run** the installer and follow setup instructions
5. **Launch** Insomnia and create an account or sign in
6. **Choose** your storage preference (Local, Cloud, or Git)

### Pricing

| Plan                                                           | Price (per user/month) billed annually |
| -------------------------------------------------------------- | -------------------------------------- |
| [**Hobby**](https://app.insomnia.rest/app/subscribe?plan=free) | **$0**                                 |
| [**Pro**](https://app.insomnia.rest/app/subscribe?plan=team)   | **$12**                                |
| [**Enterprise**](https://insomnia.rest/pricing/contact)        | **$45**                                |

_15% savings available for annual billing on Pro and Enterprise plans_

#### Plan Features Breakdown

##### Hobby (FREE)

###### Perfect for Individual Developers and Small Projects

**Core Features**:

- All API designing, debugging, mocking, and collaboration features
- Unlimited collaborators for accounts with 1 project
- Unlimited collection runs
- REST, GraphQL, SOAP, gRPC, WebSocket, SSE clients
- Pre/post-request scripting
- Collection environments and global environments
- Test writing and results
- Design specs with API spec editor and live preview

**Mock Server Usage**:

- 1,000 free mock requests per month
- Additional requests: $10 per 25,000 requests

###### Pro ($12/month)

###### Ideal for Growing Teams and Professional Development

**Everything in Hobby, plus**:

- Unlimited collaborators across all projects
- Unlimited organizations
- Native Git support for version control
- RBAC (Role-Based Access Control) and permissions
- Enhanced collaboration features

**Mock Server Usage**:

- 10,000 free mock requests per month
- Additional requests: $10 per 25,000 requests

###### Enterprise ($45/month)

###### Designed for Large Organizations with Advanced Security Needs

**Everything in Pro, plus**:

- Enterprise SSO (SAML, OIDC) & SCIM
- Storage control to mandate Cloud, Local, or Git
- Advanced RBAC for teams with granular permissions
- Domain Capture & Invite Control for security
- Self-hosted API mocking for on-premises deployment
- Enterprise support with dedicated customer success
- Custom payment options including invoicing
- Unlimited mock requests (throttled at 50 reqs/user)
- Vault integrations (HashiCorp Vault, Azure Key Vault, etc.)
- Kong Professional Services (optional add-on)

##### Feature Comparison Matrix

| Feature Category    | Hobby                 | Pro       | Enterprise             |
| ------------------- | --------------------- | --------- | ---------------------- |
| **API Debugging**   | Full                  | Full      | Full                   |
| **API Design**      | Full                  | Full      | Full                   |
| **API Testing**     | Full                  | Full      | Full                   |
| **Projects**        | 1                     | Unlimited | Unlimited              |
| **Collaborators**   | Unlimited (1 project) | Per seat  | Per seat               |
| **Git Integration** |                       |           |                        |
| **RBAC**            | Basic                 | Standard  | Advanced               |
| **SSO**             | Social                | Social    | Enterprise (SAML/OIDC) |
| **Storage Control** |                       |           |                        |
| **Support Level**   | GitHub/Email          | Email     | Enterprise             |

<BackToTop />

## Using Insomnia

### Getting Started with Your First Request

#### Creating a Request

##### Method 1: Quick Start from Scratch Pad

1. **Open Insomnia** and navigate to the **Scratch Pad**
2. **Enter your API endpoint** in the URL bar (e.g., `https://jsonplaceholder.typicode.com/posts`)
3. **Select the HTTP method** from the dropdown (GET, POST, PUT, DELETE, PATCH, etc.)
4. **Click** <Button variant="primary" className="mx-1">Send</Button> to execute the request
5. **View the response** in the response panel

###### Method 2: Create and Save to Collection

1. **Click** <Button variant="tertiary" className="mx-1">New Request</Button> in the sidebar
2. **Enter request details**:
   - Name: Descriptive request name
   - Method: HTTP method (GET, POST, etc.)
   - URL: Complete endpoint URL
3. **Select or create a collection** to organize the request
4. **Click** <Button variant="tertiary" className="mx-1">Create</Button> to save the request

##### Request Configuration Examples

**Basic GET Request**:

```http
GET https://jsonplaceholder.typicode.com/posts/1
Accept: application/json
User-Agent: Insomnia/2024.1.0
```

**POST Request with JSON Body**:

```http
POST https://jsonplaceholder.typicode.com/posts
Content-Type: application/json

{
  "title": "My New Post",
  "body": "This is the content of my post",
  "userId": 1
}
```

**Request with Authentication**:

```http
GET https://api.github.com/user
Authorization: Bearer ghp_1234567890abcdef
Accept: application/vnd.github.v3+json
```

### Working with Collections and Workspaces

#### Creating and Organizing Collections

**Creating a New Collection**:

1. **Right-click** in the sidebar or click the **+** icon
2. **Select** "New Collection"
3. **Provide collection details**:
   - Name: Descriptive collection name
   - Description: Purpose and scope
4. **Configure default settings** (optional):
   - Base URL
   - Authentication
   - Headers

**Collection Structure Example**:

```txt title="Collection Structure Example"
E-commerce API v2.0
├── Authentication
│   ├── POST User Login
│   ├── POST Refresh Token
│   └── POST User Logout
├── Product Management
│   ├── GET All Products
│   ├── GET Product by ID
│   ├── POST Create Product
│   ├── PUT Update Product
│   └── DELETE Product
├── User Management
│   ├── GET User Profile
│   ├── PUT Update Profile
│   └── DELETE User Account
└── Order Processing
    ├── POST Create Order
    ├── GET Order Status
    ├── PUT Update Order
    └── POST Cancel Order
```

##### Managing Workspaces

**Workspace Types**:

- Local Workspace: Stored locally on your machine
- Cloud Workspace: Synchronized across devices and team members
- Git Workspace: Version-controlled with Git integration

**Creating a Workspace**:

1. **Click** the workspace dropdown in the top-left
2. **Select** "Create Workspace"
3. **Choose workspace type** and configure settings
4. **Invite team members** (for cloud/git workspaces)

### Advanced Request Configuration

#### Authentication Methods

**Bearer Token**:

```http
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**API Key Authentication**:

```http
## Header-based
X-API-Key: your-api-key-here

## Query Parameter
?api_key=your-api-key-here
```

**Basic Authentication**:

```http
Authorization: Basic dXNlcm5hbWU6cGFzc3dvcmQ=
```

**OAuth 2.0 Configuration**:

1. **Select** "OAuth 2.0" in the Auth tab
2. **Configure OAuth settings**:
   - Grant Type: Authorization Code, Client Credentials, etc.
   - Authorization URL: Provider's auth endpoint
   - Access Token URL: Token exchange endpoint
   - Client ID & Secret: Application credentials
3. **Initiate OAuth flow** to obtain access token

### Environment Variables

**Creating Environments**:

1. **Click** the environment dropdown (top-right)
2. **Select** "Manage Environments"
3. **Create new environment** with variables

**Environment Example**:

```json
{
  "name": "Production",
  "variables": {
    "baseURL": "https://api.production.com",
    "apiKey": "prod_key_123456",
    "timeout": "5000",
    "version": "v2"
  }
}
```

**Using Variables in Requests**:

```http
URL: {{ _.baseURL }}/api/{{ _.version }}/users
Headers:
  Authorization: Bearer {{ _.apiKey }}
  X-Timeout: {{ _.timeout }}
```

**Environment Inheritance**:

```http
 Global Environment
   Base Environment (shared variables)
   Development (dev-specific overrides)
   Staging (staging-specific overrides)
   Production (prod-specific overrides)
```

### Testing and Automation

#### Pre-request and Post-response Scripts

Insomnia supports JavaScript scripting for dynamic request preparation and response validation.

**Pre-request Script Examples**:

```javascript
// Set dynamic timestamp
insomnia.environment.set("timestamp", new Date().toISOString());

// Generate random test data
const randomEmail = `user${Math.random().toString(36).substr(2, 9)}@example.com`;
insomnia.environment.set("testEmail", randomEmail);

// Calculate HMAC signature for API authentication
const crypto = require("crypto");
const message =
  insomnia.request.getUrl() + insomnia.environment.get("timestamp");
const signature = crypto
  .createHmac("sha256", insomnia.environment.get("secretKey"))
  .update(message)
  .digest("hex");
insomnia.environment.set("signature", signature);

// Conditional logic based on environment
if (insomnia.environment.get("env") === "production") {
  insomnia.request.addHeader("X-Environment", "production");
  insomnia.request.addHeader("X-Rate-Limit", "strict");
}
```

**Post-response Script Examples**:

```javascript
// Basic response validation
const response = insomnia.response;
insomnia.test("Status code is 200", () => {
  insomnia.expect(response.status).to.equal(200);
});

insomnia.test("Response time is acceptable", () => {
  insomnia.expect(response.responseTime).to.be.below(1000);
});

// JSON response validation
insomnia.test("Response has required fields", () => {
  const jsonData = JSON.parse(response.body);
  insomnia.expect(jsonData).to.have.property("id");
  insomnia.expect(jsonData).to.have.property("email");
  insomnia.expect(jsonData.email).to.match(/^[^\s@]+@[^\s@]+\.[^\s@]+$/);
});

// Extract data for subsequent requests
insomnia.test("Extract user ID for next request", () => {
  const jsonData = JSON.parse(response.body);
  if (jsonData.id) {
    insomnia.environment.set("userId", jsonData.id);
    insomnia.environment.set("authToken", jsonData.token);
  }
});

// Conditional testing based on response
insomnia.test("Admin user validation", () => {
  const jsonData = JSON.parse(response.body);
  if (jsonData.role === "admin") {
    insomnia.expect(jsonData.permissions).to.include("admin_access");
    insomnia.expect(jsonData.features).to.include("user_management");
  }
});
```

##### Collection Runner and Automated Testing

**Setting Up Collection Runs**:

1. **Right-click** on a collection
2. **Select** "Run Collection"
3. **Configure run settings**:
   - Environment selection
   - Iteration count
   - Delay between requests
   - Data file for parameterized testing
4. **Execute** the collection run

**Collection Run Configuration**:

```http title="Collection Run Configuration"
 Collection Run Settings
   Environment: Production
   Iterations: 3 runs
   Delay: 500ms between requests
   Data Source: test-users.csv
   Reports: JSON, JUnit XML
   Stop on Error: Disabled
```

**Data-Driven Testing with CSV**:

```csv
username,email,role,expectedStatus
john_doe,john@example.com,user,201
jane_admin,jane@example.com,admin,201
invalid_user,not-an-email,user,400
test_user,test@example.com,user,201
```

**Using CSV Data in Requests**:

```json title="CSV Data Example"
{
  "username": "{{ username }}",
  "email": "{{ email }}",
  "role": "{{ role }}",
  "password": "defaultPassword123"
}
```

##### Test Results and Reporting

**Test Results Dashboard**:

```txt title="Test Results Dashboard"
 Collection Run Results
┌─────────────────────────────────────────┐
   Collection: E-commerce API Test Suite
   Environment: Production
   Total Requests: 15
   Successful: 13 (87%)
   Failed: 2 (13%)
   Average Response Time: 325ms
   Total Duration: 00:01:45
   ───────────────────────────────────────┘

Test Results:
 POST /auth/login (234ms) - All tests passed (3/3)
 GET /products (187ms) - All tests passed (2/2)
 POST /orders (timeout) - Request timeout exceeded
 GET /orders/123 (298ms) - All tests passed (4/4)
 DELETE /users/456 (403ms) - Authorization failed (1/3)
```

### Mock Servers and API Simulation

#### Creating Mock Servers

**Setting Up a Mock Server**:

1. **Navigate** to the Design tab
2. **Create or import** an OpenAPI specification
3. **Click** "Start Mock Server"
4. **Configure mock settings**:
   - Port number
   - Response examples
   - Dynamic responses
5. **Access** your mock server at `localhost:port`

**Mock Server Configuration**:

```yaml title="Mock Server Configuration"
## Mock Server Configuration for Insomnia
## OpenAPI Specification for Mocking
openapi: 3.0.0
info:
  title: E-commerce API Mock
  version: 1.0.0
servers:
  - url: http://localhost:3001
paths:
  /products:
    get:
      responses:
        "200":
          description: List of products
          content:
            application/json:
              example:
                products:
                  - id: 1
                    name: "Sample Product"
                    price: 29.99
                  - id: 2
                    name: "Another Product"
                    price: 49.99
```

**Dynamic Mock Responses**:

```javascript title="Dynamic Mock Responses"
// Mock server script for dynamic responses
function generateMockResponse(request) {
  const productId = request.params.id;

  if (productId === "404") {
    return {
      status: 404,
      body: { error: "Product not found" },
    };
  }

  return {
    status: 200,
    body: {
      id: productId,
      name: `Product ${productId}`,
      price: Math.floor(Math.random() * 100) + 10,
      timestamp: new Date().toISOString(),
    },
  };
}
```

### Mock Server Use Cases

**Development Scenarios**:

```txt title="Mock Server Use Cases"
 Mock Server Applications
   Frontend Development
        Parallel development while backend is in progress
        UI/UX testing with consistent data
        Demo and prototype development
   Testing & QA
        Error scenario simulation
        Edge case testing
        Load testing preparation
   Documentation & Training
        Interactive API documentation
        Developer onboarding
        Client demonstrations
   Integration Testing
      Third-party service simulation
      External API dependency mocking
      Contract testing
```

### Multi-Protocol Support

#### GraphQL Integration

Insomnia provides native GraphQL support with advanced features for query development and testing.

**GraphQL Features**:

```graphql title="GraphQL Features"
 GraphQL Capabilities
   Query editor with syntax highlighting
   Schema introspection and auto-completion
   Documentation browser
   Query validation and error detection
   Variables and fragments support
   Subscription testing
   Query performance analysis
```

**GraphQL Query Example**:

```graphql title="GraphQL Query Example"
## Query with Variables and Fragments
query GetUserWithPosts($userId: ID!, $first: Int) {
  user(id: $userId) {
    ...UserDetails
    posts(first: $first) {
      edges {
        node {
          ...PostDetails
        }
      }
      pageInfo {
        hasNextPage
        endCursor
      }
    }
  }
}

fragment UserDetails on User {
  id
  name
  email
  avatar
  createdAt
}

fragment PostDetails on Post {
  id
  title
  content
  publishedAt
  tags
}
```

**GraphQL Variables**:

```json title="GraphQL Variables"
{
  "userId": "123",
  "first": 10
}
```

### WebSocket Testing

Test real-time WebSocket connections for chat applications, live updates, and streaming data.

**WebSocket Configuration**:

```http title="WebSocket Configuration"
 WebSocket Setup
   URL: ws://localhost:8080/chat
   Subprotocols: chat, notification
   Headers: Authorization, Origin
   Message Format: JSON/Text
   Auto-reconnect: Enabled
```

**WebSocket Message Examples**:

```json title="WebSocket Message Examples"
// Send chat message
{
  "type": "message",
  "channel": "general",
  "content": "Hello, world!",
  "user": "john_doe",
  "timestamp": "2024-01-15T10:30:00Z"
}

// Subscribe to notifications
{
  "type": "subscribe",
  "channels": ["notifications", "alerts"],
  "userId": "12345"
}

// Real-time response
{
  "type": "message_received",
  "messageId": "msg_789",
  "channel": "general",
  "author": "jane_doe",
  "content": "Hi there!",
  "timestamp": "2024-01-15T10:30:05Z"
}
```

#### gRPC Support

Insomnia supports gRPC protocol for high-performance, strongly-typed API communication.

**gRPC Configuration**:

```txt title="gRPC Configuration"
 gRPC Setup
   Server URL: grpc://localhost:50051
   Proto File: user_service.proto
   Authentication: TLS certificates
   Streaming: Unary, Server, Client, Bidirectional
   Metadata: Custom headers and context
```

**Proto Definition Example**:

```txt
// user_service.proto
syntax = "proto3";

package user;

service UserService {
  rpc GetUser(GetUserRequest) returns (User);
  rpc CreateUser(CreateUserRequest) returns (User);
  rpc StreamUsers(StreamUsersRequest) returns (stream User);
}

message User {
  string id = 1;
  string name = 2;
  string email = 3;
  int64 created_at = 4;
}

message GetUserRequest {
  string id = 1;
}
```

### Code Generation and Export

#### Supported Languages and Frameworks

Insomnia can generate code snippets in 20+ programming languages for easy integration.

**Available Code Generators**:

```txt title="Code Generation Support"
 Code Generation Support
  JavaScript
        Fetch API
        Axios
        jQuery
        Node.js (HTTP/HTTPS)
  Python
        Requests
        urllib3
        http.client
  Java
        OkHttp
        Unirest
        HttpURLConnection
  C#
        HttpClient
        RestSharp
        WebRequest
  PHP
        cURL
        Guzzle
        file_get_contents
  Go (net/http)
  Ruby (Net::HTTP)
  Swift (URLSession)
  Kotlin (OkHttp)
  Rust (reqwest)
  PowerShell
  cURL
```

**Code Generation Examples**:

**JavaScript (Fetch)**:

```javascript title="JavaScript Fetch Example"
fetch("https://api.example.com/users", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    Authorization: "Bearer your-token-here",
  },
  body: JSON.stringify({
    name: "John Doe",
    email: "john@example.com",
    role: "user",
  }),
})
  .then((response) => response.json())
  .then((data) => console.log(data))
  .catch((error) => console.error("Error:", error));
```

**Python (Requests)**:

```python title="Python Requests Example"
import requests
import json

url = "https://api.example.com/users"
headers = {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer your-token-here'
}
data = {
    "name": "John Doe",
    "email": "john@example.com",
    "role": "user"
}

response = requests.post(url, headers=headers, data=json.dumps(data))
print(response.json())
```

##### Import and Export Capabilities

**Import Sources**:

```txt title="Import Sources"
 Import Support
   Postman Collections (v1, v2, v2.1)
   OpenAPI/Swagger (2.0, 3.0)
   HAR (HTTP Archive) files
   cURL commands
   Insomnia collections
   API Blueprint
   WSDL (SOAP services)
```

**Export Formats**:

```txt title="Export Formats"
 Export Options
   Insomnia format (.json)
   OpenAPI 3.0 specification
   HAR format
   cURL commands
   Collection run results
   API documentation (HTML)
```

<BackToTop />

## Plugins and Extensions

Insomnia supports a rich ecosystem of plugins to extend functionality and customize workflows. The Plugin Hub provides both official and community-contributed plugins for various use cases.

### Popular and Essential Plugins

#### Workflow Enhancement Plugins

##### [Run Requests Parallel](https://insomnia.rest/plugins/insomnia-plugin-run-requests-parallel)

- Execute multiple requests simultaneously
- Configurable parallel vs sequential execution
- Performance testing capabilities

###### � [Run All Requests](https://insomnia.rest/plugins/insomnia-plugin-run-all-requests)

- Execute all requests in a collection sequentially
- Automated testing workflows
- Collection-wide execution

###### � [Query Plugin](https://insomnia.rest/plugins/insomnia-plugin-query)

- Search and filter request/response data
- JSONPath and XPath query support
- Advanced data extraction capabilities

###### [Conditional Selector](https://insomnia.rest/plugins/insomnia-plugin-conditional-selector)

- Conditional request execution
- Dynamic workflow control
- Logic-based automation

##### Security and Authentication Plugins

###### [totp Plugin](https://insomnia.rest/plugins/insomnia-plugin-totp)

- Generate Time-Based One-Time Passwords
- MFA secret integration
- 2FA testing support

###### [Better JWT](https://insomnia.rest/plugins/insomnia-plugin-better-jwt)

- Generate and decode JWT tokens
- Token validation and testing
- Custom claims and signing algorithms

###### [1Password Plugin](https://insomnia.rest/plugins/insomnia-plugin-op)

- Integrate with 1Password for secure credentials
- Automated secret management
- Secure password and token storage

##### Development and Scripting Plugins

###### [Better Python](https://insomnia.rest/plugins/insomnia-plugin-better-python)

- Execute Python scripts within Insomnia
- Data processing and transformation
- Custom logic implementation

###### [Terminal Command](https://insomnia.rest/plugins/insomnia-plugin-terminal-command)

- Run terminal commands from Insomnia
- System integration capabilities
- Custom automation scripts

###### [yaml as JSON](https://insomnia.rest/plugins/insomnia-plugin-yaml-as-json)

- Convert YAML request bodies to JSON
- Seamless YAML workflow support
- Format transformation

###### [Fake Data Generator](https://insomnia.rest/plugins/insomnia-plugin-fake)

- Generate realistic fake data for testing
- Multiple data types and formats
- Customizable data patterns

###### [Quicky Fake Data Generation](https://insomnia.rest/plugins/insomnia-plugin-quicky-fake-data-generation)

- Quick fake data generation utilities
- Instant test data creation
- Streamlined workflow integration

###### [ulid Generator](https://insomnia.rest/plugins/insomnia-plugin-ulid-generator)

- Generate Universally Unique Lexicographically Sortable Identifiers
- Timestamp-based unique IDs
- Database-friendly identifier generation

###### GraphQL Query Generator](https://insomnia.rest/plugins/insomnia-plugin-graphql-query-generator)

- Auto-generate GraphQL queries from schema
- Schema-based query construction
- Rapid GraphQL development

##### Version Control and Collaboration

###### [git Integration](https://insomnia.rest/plugins/insomnia-plugin-git-integration)

- Enhanced Git repository synchronization
- Commit and branch management
- Merge conflict resolution

###### [Profile Picker](https://insomnia.rest/plugins/insomnia-plugin-profile-picker)

- Manage dynamic user profiles
- Profile-based data injection
- Multi-user testing scenarios

###### [User Picker](https://insomnia.rest/plugins/insomnia-plugin-user-picker)

- Quick user profile switching
- User-specific request configuration
- Team collaboration enhancement

##### Testing and Validation Plugins

###### [Response Validator](https://insomnia.rest/plugins/insomnia-plugin-response-validator)

- Advanced response validation
- Schema compliance testing
- Custom validation rules

###### [Count in Page](https://insomnia.rest/plugins/insomnia-plugin-count-in-page)

- Count regex or string occurrences
- Response data analysis
- Pattern matching capabilities

##### Theming and UI Plugins

###### [Min Theme](https://insomnia.rest/plugins/insomnia-plugin-min-theme)

- Minimal, clean interface design
- Reduced visual clutter
- Focus-oriented layout

###### [Dark Modern Theme](https://insomnia.rest/plugins/insomnia-plugin-dark-modern-theme)

- VSCode Dark Theme inspired design
- Modern development aesthetic
- Eye-strain reduction

###### [Retro Vibe Theme](https://insomnia.rest/plugins/insomnia-plugin-retro-vibe-theme)

- Nostalgic retro-inspired design
- Unique color palette
- Vintage development aesthetic

###### GitHub Theme](https://insomnia.rest/plugins/insomnia-plugin-github-theme)

- GitHub-inspired color scheme
- Familiar development environment
- Clean, professional appearance

##### Advanced Feature Plugins

###### [Feature Time](https://insomnia.rest/plugins/insomnia-plugin-feature-time)

- Time manipulation template tags
- Date arithmetic operations
- Timestamp generation utilities

###### [Global Headers](https://insomnia.rest/plugins/insomnia-plugin-global-headers)

- Automatically apply headers to all requests
- Global authentication headers
- Consistent header management

###### [gRPC Stream to File](https://insomnia.rest/plugins/insomnia-plugin-grpc-stream-to-file)

- Stream gRPC responses to files
- Large data handling
- Performance optimization

###### [GenAI Image Preview](https://insomnia.rest/plugins/insomnia-plugin-genai-image-preview)

- Preview AI-generated images in responses
- Visual content validation
- Enhanced multimedia API testing

### Plugin Installation and Management

**Installing Plugins**:

1. **Navigate** to Settings → Plugins
2. **Browse** available plugins or search by name
3. **Click** "Install" on desired plugins
4. **Restart** Insomnia if required

**Managing Installed Plugins**:

```txt title="Plugin Management"
 Plugin Management
   View installed plugins
   Update plugins to latest versions
   Configure plugin settings
   Disable/enable plugins
   Uninstall unused plugins
```

**Creating Custom Plugins**:

```javascript title="Custom Plugin Example"
// Example custom plugin structure
module.exports.templateTags = [
  {
    name: "customFunction",
    displayName: "Custom Function",
    description: "A custom template tag example",
    args: [
      {
        displayName: "Input Value",
        type: "string",
        placeholder: "Enter value...",
      },
    ],
    async run(context, value) {
      // Custom logic here
      return `Processed: ${value}`;
    },
  },
];
```

### Best Practices and Recommendations

**Essential Plugin Stack**:

```txt title="Recommended Plugin Setup"
 Recommended Plugin Setup
   Security
        HashiCorp Vault or 1Password
        TOTP Plugin for MFA
        Better JWT for token management
   Testing
        Response Validator
        Run Requests Parallel
        Better Python for scripting
   Themes
        Choose based on preference
        Dark Modern for VS Code users
        Catppuccin for modern aesthetics
   Productivity
      Requests Navigator
      Query Plugin
      Global Headers
```

**Plugin Configuration Tips**:

- Regular Updates: Keep plugins updated for security and features
- Performance: Monitor plugin impact on Insomnia performance
- Security: Verify plugin sources and permissions
- Team Consistency: Standardize plugin usage across teams
- Documentation: Document custom plugin configurations

<BackToTop />
