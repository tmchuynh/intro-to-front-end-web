import BackToTop from "@/components/BackToTop";

# Error Page

## Table of Contents

## About This Page

This page is designed to handle errors that may occur during the operation of the portfolio website. It provides a user-friendly message and a link to return to the home page. It is essential for maintaining a good user experience by guiding users back to a functional part of the site when an error occurs.

Even though this page is not directly linked to any specific functionality, it serves as a fallback for all error cases in the application. This ensures that users are not met with a blank screen or an unhelpful error message when something goes wrong.

This page comes up when the user tries to access a page that does not exist or when an unexpected error occurs in the application. It is a crucial part of the portfolio website's error handling strategy.

## What You Should Include

- A user-friendly message indicating that an error has occurred.
- A link back to the home page or other relevant sections of the site.
- Contact information or a link to support for users who may need assistance.
- Optional: A search bar or links to popular sections of the site to help users navigate.
- A clear call to action, such as "Return to Home" or "Try Again".
- Optional: A button to report the error or provide feedback.
- Optional: A link to the documentation or help section for further assistance.

##### IMPORTANT CONSIDERATIONS

- Ensure that the error message is clear and concise.
- Make sure the link back to the home page is prominently displayed.
- Consider adding a search bar or links to popular sections to help users find what they are looking for.
- Test the error page to ensure it works correctly in various scenarios, such as when the site is down or when a specific page cannot be found.
- Keep the design consistent with the rest of the portfolio website to maintain a cohesive user experience.
  <BackToTop />

## Examples

### Example #1: Minimalistic Error Page

A clean and simple error page that focuses on clarity and quick recovery options.

```tsx
import React from "react";
import Link from "next/link";

interface ErrorPageProps {
  error?: Error & { digest?: string };
  reset?: () => void;
}

export default function MinimalisticError({ error, reset }: ErrorPageProps) {
  return (
    <div className="min-h-screen bg-white flex items-center justify-center px-6">
      <div className="max-w-md w-full text-center">
        {/* Error Icon */}
        <div className="mb-8">
          <div className="w-24 h-24 mx-auto bg-red-100 rounded-full flex items-center justify-center">
            <span className="text-4xl text-red-600">⚠️</span>
          </div>
        </div>

        {/* Error Message */}
        <div className="mb-8">
          <h1 className="text-3xl font-bold text-slate-900 mb-4">
            Something went wrong
          </h1>
          <p className="text-lg text-slate-600 mb-6">
            We apologize for the inconvenience. An unexpected error has
            occurred.
          </p>

          {/* Development error details */}
          {process.env.NODE_ENV === "development" && error && (
            <div className="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg text-left">
              <h3 className="text-sm font-semibold text-red-800 mb-2">
                Development Error Details:
              </h3>
              <pre className="text-xs text-red-700 overflow-auto">
                {error.message}
              </pre>
            </div>
          )}
        </div>

        {/* Action Buttons */}
        <div className="space-y-4">
          {reset && (
            <button
              onClick={reset}
              className="w-full bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors font-semibold"
            >
              Try Again
            </button>
          )}

          <Link
            href="/"
            className="block w-full bg-slate-100 text-slate-700 px-6 py-3 rounded-lg hover:bg-slate-200 transition-colors font-semibold"
          >
            Return to Home
          </Link>
        </div>

        {/* Additional Help */}
        <div className="mt-8 pt-6 border-t border-slate-200">
          <p className="text-sm text-slate-500 mb-3">
            If this problem persists, please contact us:
          </p>
          <a
            href="mailto:support@example.com"
            className="text-blue-600 hover:text-blue-700 text-sm font-medium"
          >
            support@example.com
          </a>
        </div>

        {/* Error ID for support */}
        {error?.digest && (
          <div className="mt-4 text-xs text-slate-400">
            Error ID: {error.digest}
          </div>
        )}
      </div>
    </div>
  );
}
```

#### Customization Options

- Error messages: Customize the text to match your brand voice and tone
- Colors: Update the color scheme to match your site's branding
- Contact information: Replace with your actual support email or contact form
- Error tracking: Integrate with error monitoring services like Sentry
- Retry logic: Add custom retry functionality specific to your application

<BackToTop />

### Example #2: Detailed Error Page with Support Features

A error page with detailed error information, reporting capabilities, and multiple recovery options.

```tsx
import React, { useState, useEffect } from "react";
import Link from "next/link";

interface ErrorPageProps {
  error?: Error & { digest?: string };
  reset?: () => void;
}

interface ErrorReport {
  userAgent: string;
  timestamp: string;
  url: string;
  errorMessage: string;
  userDescription: string;
}

export default function DetailedError({ error, reset }: ErrorPageProps) {
  const [isReporting, setIsReporting] = useState(false);
  const [reportSubmitted, setReportSubmitted] = useState(false);
  const [userDescription, setUserDescription] = useState("");
  const [searchQuery, setSearchQuery] = useState("");
  const [showErrorDetails, setShowErrorDetails] = useState(false);

  const errorCode =
    error?.digest || `ERR_${Date.now().toString(36).toUpperCase()}`;
  const timestamp = new Date().toISOString();

  // Mock search suggestions - replace with real search functionality
  const searchSuggestions = [
    { title: "Home Page", href: "/", description: "Return to the main page" },
    {
      title: "Portfolio",
      href: "/portfolio",
      description: "View our work samples",
    },
    {
      title: "About Us",
      href: "/about",
      description: "Learn more about our team",
    },
    { title: "Contact", href: "/contact", description: "Get in touch with us" },
    {
      title: "Help Center",
      href: "/help",
      description: "Find answers to common questions",
    },
  ];

  const filteredSuggestions = searchQuery
    ? searchSuggestions.filter(
        (suggestion) =>
          suggestion.title.toLowerCase().includes(searchQuery.toLowerCase()) ||
          suggestion.description
            .toLowerCase()
            .includes(searchQuery.toLowerCase()),
      )
    : searchSuggestions.slice(0, 3);

  const submitErrorReport = async () => {
    setIsReporting(true);

    const errorReport: ErrorReport = {
      userAgent:
        typeof window !== "undefined" ? navigator.userAgent : "Unknown",
      timestamp,
      url: typeof window !== "undefined" ? window.location.href : "Unknown",
      errorMessage: error?.message || "Unknown error",
      userDescription,
    };

    try {
      // Simulate API call to submit error report
      await new Promise((resolve) => setTimeout(resolve, 1500));
      console.log("Error report submitted:", errorReport);
      setReportSubmitted(true);
    } catch (err) {
      console.error("Failed to submit error report:", err);
    } finally {
      setIsReporting(false);
    }
  };

  const copyErrorDetails = () => {
    const details = `
Error Code: ${errorCode}
Timestamp: ${timestamp}
URL: ${typeof window !== "undefined" ? window.location.href : "N/A"}
User Agent: ${typeof window !== "undefined" ? navigator.userAgent : "N/A"}
Error Message: ${error?.message || "Unknown error"}
    `.trim();

    navigator.clipboard?.writeText(details);
  };

  return (
    <div className="min-h-screen bg-gradient-to-br from-red-50 to-orange-50">
      {/* Header */}
      <div className="bg-white shadow-sm border-b">
        <div className="max-w-4xl mx-auto px-6 py-4">
          <div className="flex items-center justify-between">
            <Link href="/" className="text-xl font-bold text-slate-900">
              Your Logo
            </Link>
            <div className="text-sm text-slate-500">
              Error Code: {errorCode}
            </div>
          </div>
        </div>
      </div>

      <div className="max-w-4xl mx-auto px-6 py-12">
        <div className="grid lg:grid-cols-3 gap-8">
          {/* Main Error Content */}
          <div className="lg:col-span-2 space-y-8">
            {/* Error Message */}
            <div className="bg-white rounded-xl shadow-lg p-8">
              <div className="flex items-start space-x-4">
                <div className="flex-shrink-0">
                  <div className="w-12 h-12 bg-red-100 rounded-full flex items-center justify-center">
                    <span className="text-2xl">🚨</span>
                  </div>
                </div>
                <div className="flex-1">
                  <h1 className="text-2xl font-bold text-slate-900 mb-2">
                    Application Error
                  </h1>
                  <p className="text-lg text-slate-600 mb-4">
                    We encountered an unexpected error while processing your
                    request. Our team has been notified and is working to
                    resolve this issue.
                  </p>

                  {/* Action Buttons */}
                  <div className="flex flex-wrap gap-3">
                    {reset && (
                      <button
                        onClick={reset}
                        className="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors font-semibold"
                      >
                        Try Again
                      </button>
                    )}
                    <Link
                      href="/"
                      className="bg-slate-100 text-slate-700 px-6 py-3 rounded-lg hover:bg-slate-200 transition-colors font-semibold"
                    >
                      Return to Home
                    </Link>
                    <button
                      onClick={() => window.location.reload()}
                      className="bg-white text-slate-700 px-6 py-3 rounded-lg hover:bg-slate-50 transition-colors font-semibold border border-slate-300"
                    >
                      Refresh Page
                    </button>
                  </div>
                </div>
              </div>
            </div>

            {/* Search Alternative Content */}
            <div className="bg-white rounded-xl shadow-lg p-8">
              <h2 className="text-xl font-bold text-slate-900 mb-4">
                Find What You're Looking For
              </h2>
              <div className="mb-6">
                <input
                  type="text"
                  placeholder="Search our site..."
                  value={searchQuery}
                  onChange={(e) => setSearchQuery(e.target.value)}
                  className="w-full px-4 py-3 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                />
              </div>

              <div className="space-y-3">
                {filteredSuggestions.map((suggestion, index) => (
                  <Link
                    key={index}
                    href={suggestion.href}
                    className="block p-4 border border-slate-200 rounded-lg hover:border-blue-300 hover:bg-blue-50 transition-colors group"
                  >
                    <div className="flex items-center justify-between">
                      <div>
                        <h3 className="font-semibold text-slate-900 group-hover:text-blue-700">
                          {suggestion.title}
                        </h3>
                        <p className="text-sm text-slate-600">
                          {suggestion.description}
                        </p>
                      </div>
                      <span className="text-slate-400 group-hover:text-blue-500">
                        →
                      </span>
                    </div>
                  </Link>
                ))}
              </div>
            </div>

            {/* Error Details for Development */}
            {process.env.NODE_ENV === "development" && error && (
              <div className="bg-white rounded-xl shadow-lg p-8">
                <div className="flex items-center justify-between mb-4">
                  <h2 className="text-xl font-bold text-slate-900">
                    Error Details (Development)
                  </h2>
                  <button
                    onClick={() => setShowErrorDetails(!showErrorDetails)}
                    className="text-sm text-blue-600 hover:text-blue-700"
                  >
                    {showErrorDetails ? "Hide" : "Show"} Details
                  </button>
                </div>

                {showErrorDetails && (
                  <div className="space-y-4">
                    <div className="bg-red-50 border border-red-200 rounded-lg p-4">
                      <h3 className="text-sm font-semibold text-red-800 mb-2">
                        Error Message:
                      </h3>
                      <pre className="text-sm text-red-700 overflow-auto whitespace-pre-wrap">
                        {error.message}
                      </pre>
                    </div>

                    {error.stack && (
                      <div className="bg-slate-50 border border-slate-200 rounded-lg p-4">
                        <h3 className="text-sm font-semibold text-slate-800 mb-2">
                          Stack Trace:
                        </h3>
                        <pre className="text-xs text-slate-600 overflow-auto whitespace-pre-wrap">
                          {error.stack}
                        </pre>
                      </div>
                    )}

                    <button
                      onClick={copyErrorDetails}
                      className="text-sm bg-slate-100 text-slate-700 px-4 py-2 rounded hover:bg-slate-200 transition-colors"
                    >
                      Copy Error Details
                    </button>
                  </div>
                )}
              </div>
            )}
          </div>

          {/* Sidebar */}
          <div className="space-y-8">
            {/* Report Error */}
            <div className="bg-white rounded-xl shadow-lg p-6">
              <h3 className="text-lg font-bold text-slate-900 mb-4">
                Report This Error
              </h3>

              {reportSubmitted ? (
                <div className="text-center py-4">
                  <div className="text-4xl mb-2">✅</div>
                  <p className="text-green-700 font-semibold mb-2">
                    Report Submitted
                  </p>
                  <p className="text-sm text-slate-600">
                    Thank you for helping us improve!
                  </p>
                </div>
              ) : (
                <div className="space-y-4">
                  <p className="text-sm text-slate-600">
                    Help us fix this issue by providing additional details.
                  </p>

                  <textarea
                    placeholder="What were you trying to do when this error occurred? (optional)"
                    value={userDescription}
                    onChange={(e) => setUserDescription(e.target.value)}
                    rows={4}
                    className="w-full px-3 py-2 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent text-sm resize-none"
                  />

                  <button
                    onClick={submitErrorReport}
                    disabled={isReporting}
                    className="w-full bg-red-600 text-white px-4 py-3 rounded-lg hover:bg-red-700 disabled:opacity-50 disabled:cursor-not-allowed transition-colors font-semibold"
                  >
                    {isReporting ? "Submitting..." : "Submit Report"}
                  </button>
                </div>
              )}
            </div>

            {/* Quick Links */}
            <div className="bg-blue-50 rounded-xl border border-blue-200 p-6">
              <h3 className="text-lg font-bold text-slate-900 mb-4">
                Need Help?
              </h3>
              <div className="space-y-3">
                <Link
                  href="/help"
                  className="block text-blue-600 hover:text-blue-700 font-medium"
                >
                  📚 Help Center
                </Link>
                <Link
                  href="/contact"
                  className="block text-blue-600 hover:text-blue-700 font-medium"
                >
                  💬 Contact Support
                </Link>
                <Link
                  href="/faq"
                  className="block text-blue-600 hover:text-blue-700 font-medium"
                >
                  ❓ FAQ
                </Link>
                <a
                  href="mailto:support@example.com"
                  className="block text-blue-600 hover:text-blue-700 font-medium"
                >
                  📧 Email Support
                </a>
              </div>
            </div>

            {/* System Status */}
            <div className="bg-green-50 rounded-xl border border-green-200 p-6">
              <h3 className="text-lg font-bold text-slate-900 mb-2">
                System Status
              </h3>
              <div className="flex items-center space-x-2 text-sm">
                <div className="w-2 h-2 bg-green-500 rounded-full"></div>
                <span className="text-green-700">All systems operational</span>
              </div>
              <Link
                href="/status"
                className="text-sm text-green-600 hover:text-green-700 mt-2 inline-block"
              >
                View status page →
              </Link>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
```

#### Customization Options

- Error reporting: Integrate with real error tracking services (Sentry, Bugsnag, etc.)
- Search functionality: Connect to your actual search API or static site search
- Support links: Update with your real help center, FAQ, and contact pages
- System status: Connect to actual status page or monitoring services
- Branding: Customize colors, logo, and messaging to match your brand
- Analytics: Add error tracking and user behavior analytics

<BackToTop />

### Example #3: Animated Error Page with Engaging Graphics

A visually appealing error page with animations and friendly graphics to reduce user frustration and provide an engaging experience.

```tsx
import React, { useState, useEffect } from "react";
import Link from "next/link";

interface ErrorPageProps {
  error?: Error & { digest?: string };
  reset?: () => void;
}

export default function AnimatedError({ error, reset }: ErrorPageProps) {
  const [isVisible, setIsVisible] = useState(false);
  const [currentMessage, setCurrentMessage] = useState(0);
  const [showBalloons, setShowBalloons] = useState(false);
  const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 });

  const humorousMessages = [
    "Oops! Our hamster fell off the wheel 🐹",
    "The server took an unexpected coffee break ☕",
    "We're temporarily lost in cyberspace 🌌",
    "Houston, we have a problem 🚀",
    "The pixels are staging a revolt 🎨",
    "Something went sideways in the matrix 🔀",
  ];

  const encouragingMessages = [
    "Don't worry, we're on it! 💪",
    "Every error is a step toward perfection ✨",
    "Thanks for your patience! 🙏",
    "We'll have this fixed in no time ⚡",
    "Your feedback helps us improve 📈",
  ];

  useEffect(() => {
    setIsVisible(true);

    // Cycle through messages
    const messageInterval = setInterval(() => {
      setCurrentMessage((prev) => (prev + 1) % humorousMessages.length);
    }, 3000);

    // Mouse tracking for interactive elements
    const handleMouseMove = (e: MouseEvent) => {
      setMousePosition({ x: e.clientX, y: e.clientY });
    };

    window.addEventListener("mousemove", handleMouseMove);

    return () => {
      clearInterval(messageInterval);
      window.removeEventListener("mousemove", handleMouseMove);
    };
  }, []);

  const triggerBalloons = () => {
    setShowBalloons(true);
    setTimeout(() => setShowBalloons(false), 3000);
  };

  const errorCode =
    error?.digest || `ERR_${Date.now().toString(36).toUpperCase()}`;

  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-100 via-pink-50 to-indigo-100 overflow-hidden relative">
      {/* Floating Background Elements */}
      <div className="absolute inset-0 overflow-hidden pointer-events-none">
        {[...Array(8)].map((_, i) => (
          <div
            key={i}
            className="absolute opacity-20"
            style={{
              left: `${10 + i * 12}%`,
              top: `${10 + (i % 3) * 30}%`,
              animation: `float ${3 + (i % 3)}s ease-in-out infinite`,
              animationDelay: `${i * 0.5}s`,
            }}
          >
            <div className="text-4xl">
              {["🌟", "⭐", "✨", "💫", "🎈", "🎪", "🎭", "🎨"][i]}
            </div>
          </div>
        ))}
      </div>

      {/* Balloon Animation */}
      {showBalloons && (
        <div className="absolute inset-0 pointer-events-none">
          {[...Array(15)].map((_, i) => (
            <div
              key={i}
              className="absolute animate-balloon text-2xl"
              style={{
                left: `${Math.random() * 100}%`,
                animationDelay: `${Math.random() * 1}s`,
                animationDuration: `${2 + Math.random() * 2}s`,
              }}
            >
              🎈
            </div>
          ))}
        </div>
      )}

      {/* Interactive Eyes that Follow Mouse */}
      <div className="absolute top-20 left-20 pointer-events-none">
        <div className="relative">
          <div className="text-6xl">👀</div>
          <div
            className="absolute top-4 left-4 w-4 h-4 bg-black rounded-full transition-transform duration-200"
            style={{
              transform: `translate(${Math.min(Math.max((mousePosition.x - 100) / 50, -5), 5)}px, ${Math.min(Math.max((mousePosition.y - 100) / 50, -3), 3)}px)`,
            }}
          />
        </div>
      </div>

      <div className="min-h-screen flex items-center justify-center px-6 relative z-10">
        <div
          className={`max-w-2xl w-full text-center transition-all duration-1000 transform ${
            isVisible ? "translate-y-0 opacity-100" : "translate-y-10 opacity-0"
          }`}
        >
          {/* Animated Error Robot */}
          <div className="mb-8 relative">
            <div className="inline-block relative">
              <div
                className="text-8xl animate-bounce"
                style={{ animationDuration: "2s" }}
              >
                🤖
              </div>
              {/* Robot Speech Bubble */}
              <div className="absolute -top-4 -right-8 bg-white rounded-lg px-3 py-1 shadow-lg border-2 border-slate-200 animate-pulse">
                <div className="text-xs text-slate-600">Beep boop!</div>
                <div className="absolute bottom-0 left-4 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-white"></div>
              </div>
            </div>

            {/* Glitch Effect */}
            <div className="mt-4 relative">
              <div className="text-4xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-red-500 via-yellow-500 to-purple-500 animate-pulse">
                ERROR
              </div>
              <div className="absolute inset-0 text-4xl font-bold text-red-500 opacity-50 animate-ping">
                ERROR
              </div>
            </div>
          </div>

          {/* Dynamic Humorous Messages */}
          <div className="mb-8">
            <h1
              className={`text-3xl font-bold text-slate-800 mb-4 transition-all duration-500 ${
                isVisible
                  ? "translate-y-0 opacity-100"
                  : "translate-y-4 opacity-0"
              }`}
            >
              {humorousMessages[currentMessage]}
            </h1>
            <p className="text-lg text-slate-600 mb-4">
              {encouragingMessages[currentMessage % encouragingMessages.length]}
            </p>
          </div>

          {/* Loading Bar Animation */}
          <div className="mb-8">
            <div className="bg-white bg-opacity-50 rounded-full h-4 overflow-hidden border-2 border-white">
              <div className="bg-gradient-to-r from-purple-500 via-pink-500 to-red-500 h-full rounded-full animate-loading-bar"></div>
            </div>
            <p className="text-sm text-slate-600 mt-2 animate-pulse">
              Channeling positive vibes to fix this...
            </p>
          </div>

          {/* Interactive Action Buttons */}
          <div className="space-y-4 mb-8">
            {reset && (
              <button
                onClick={reset}
                className="w-full bg-gradient-to-r from-purple-600 to-pink-600 text-white px-8 py-4 rounded-xl hover:from-purple-700 hover:to-pink-700 transition-all duration-300 transform hover:scale-105 font-semibold shadow-lg hover:shadow-xl"
              >
                🔄 Try the Magic Again
              </button>
            )}

            <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <Link
                href="/"
                className="bg-white bg-opacity-80 backdrop-blur-sm text-slate-700 px-6 py-3 rounded-xl hover:bg-opacity-100 transition-all duration-300 transform hover:scale-105 font-semibold shadow-md border border-white"
              >
                🏠 Beam Me Home
              </Link>

              <button
                onClick={triggerBalloons}
                className="bg-white bg-opacity-80 backdrop-blur-sm text-slate-700 px-6 py-3 rounded-xl hover:bg-opacity-100 transition-all duration-300 transform hover:scale-105 font-semibold shadow-md border border-white"
              >
                🎈 Cheer Me Up!
              </button>
            </div>
          </div>

          {/* Mood Meter */}
          <div className="bg-white bg-opacity-70 backdrop-blur-sm rounded-xl p-6 border border-white border-opacity-50 mb-8">
            <h3 className="text-lg font-semibold text-slate-800 mb-4">
              How are you feeling about this error?
            </h3>
            <div className="flex justify-center space-x-4">
              {[
                { emoji: "😭", label: "Frustrated" },
                { emoji: "😕", label: "Confused" },
                { emoji: "😐", label: "Meh" },
                { emoji: "😊", label: "Understanding" },
                { emoji: "😂", label: "Amused" },
              ].map((mood, index) => (
                <button
                  key={index}
                  className="text-3xl hover:scale-125 transition-transform duration-200 p-2 rounded-full hover:bg-white hover:bg-opacity-50"
                  title={mood.label}
                  onClick={() => console.log(`User mood: ${mood.label}`)}
                >
                  {mood.emoji}
                </button>
              ))}
            </div>
          </div>

          {/* Fun Facts */}
          <div className="bg-gradient-to-r from-blue-100 to-purple-100 rounded-xl p-6 border border-white border-opacity-50">
            <h3 className="text-lg font-semibold text-slate-800 mb-3">
              🎯 Did You Know?
            </h3>
            <p className="text-slate-600 text-sm mb-3">
              The first computer bug was an actual bug! In 1947, Grace Hopper
              found a moth trapped in a computer relay.
            </p>
            <div className="flex items-center justify-between text-xs text-slate-500">
              <span>Error Code: {errorCode}</span>
              <span>Status: Learning from mistakes 📚</span>
            </div>
          </div>

          {/* Contact Support with Style */}
          <div className="mt-8 text-center">
            <p className="text-sm text-slate-600 mb-3">
              Still stuck? Our friendly humans are here to help!
            </p>
            <div className="flex justify-center space-x-4">
              <Link
                href="/contact"
                className="text-blue-600 hover:text-blue-700 font-medium text-sm bg-white bg-opacity-50 px-4 py-2 rounded-full border border-blue-200 hover:bg-opacity-80 transition-all"
              >
                💬 Chat with Support
              </Link>
              <a
                href="mailto:help@example.com"
                className="text-purple-600 hover:text-purple-700 font-medium text-sm bg-white bg-opacity-50 px-4 py-2 rounded-full border border-purple-200 hover:bg-opacity-80 transition-all"
              >
                📧 Send Email
              </a>
            </div>
          </div>
        </div>
      </div>

      {/* Custom CSS Animations */}
      <style jsx>{`
        @keyframes float {
          0%,
          100% {
            transform: translateY(0px) rotate(0deg);
          }
          50% {
            transform: translateY(-20px) rotate(5deg);
          }
        }

        @keyframes balloon {
          0% {
            transform: translateY(100vh) scale(0);
            opacity: 0;
          }
          10% {
            opacity: 1;
          }
          90% {
            opacity: 1;
          }
          100% {
            transform: translateY(-20vh) scale(1.2);
            opacity: 0;
          }
        }

        @keyframes loading-bar {
          0% {
            transform: translateX(-100%);
          }
          50% {
            transform: translateX(0%);
          }
          100% {
            transform: translateX(100%);
          }
        }

        .animate-balloon {
          animation: balloon linear forwards;
        }

        .animate-loading-bar {
          animation: loading-bar 3s ease-in-out infinite;
        }
      `}</style>
    </div>
  );
}
```

#### Customization Options

- Animations: Adjust timing, easing, and effects for different feels
- Messages: Customize humorous and encouraging messages to match your brand personality
- Interactive elements: Add more interactive features like games or puzzles
- Color schemes: Modify gradients and colors to align with your design system
- Characters: Replace the robot with custom illustrations or brand mascots
- Sound effects: Add audio feedback for interactions (with user consent)

<BackToTop />

### Example #4: Technical Error Page with Debug Information

A error page designed for technical users and development environments with detailed debugging information.

```tsx
import React, { useState, useEffect } from "react";
import Link from "next/link";

interface ErrorPageProps {
  error?: Error & { digest?: string };
  reset?: () => void;
}

interface ErrorContext {
  timestamp: string;
  userAgent: string;
  url: string;
  referrer: string;
  viewport: string;
  errorBoundary: string;
  componentStack?: string;
}

export default function TechnicalError({ error, reset }: ErrorPageProps) {
  const [errorContext, setErrorContext] = useState<ErrorContext | null>(null);
  const [copied, setCopied] = useState(false);
  const [showFullStack, setShowFullStack] = useState(false);
  const [retryCount, setRetryCount] = useState(0);

  useEffect(() => {
    // Collect error context information
    const context: ErrorContext = {
      timestamp: new Date().toISOString(),
      userAgent: navigator.userAgent,
      url: window.location.href,
      referrer: document.referrer || "Direct access",
      viewport: `${window.innerWidth}x${window.innerHeight}`,
      errorBoundary: "React Error Boundary",
      componentStack: error?.stack,
    };
    setErrorContext(context);
  }, [error]);

  const copyErrorReport = async () => {
    const errorReport = {
      error: {
        message: error?.message,
        stack: error?.stack,
        digest: error?.digest,
      },
      context: errorContext,
      retryCount,
      buildInfo: {
        env: process.env.NODE_ENV,
        version: process.env.NEXT_PUBLIC_APP_VERSION || "unknown",
      },
    };

    try {
      await navigator.clipboard.writeText(JSON.stringify(errorReport, null, 2));
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    } catch (err) {
      console.error("Failed to copy error report:", err);
    }
  };

  const handleRetry = () => {
    setRetryCount((prev) => prev + 1);
    if (reset) {
      reset();
    }
  };

  const errorCode =
    error?.digest || `ERR_${Date.now().toString(36).toUpperCase()}`;

  return (
    <div className="min-h-screen bg-slate-900 text-white">
      {/* Header */}
      <div className="bg-red-600 text-white">
        <div className="max-w-7xl mx-auto px-6 py-4">
          <div className="flex items-center justify-between">
            <div className="flex items-center space-x-3">
              <span className="text-2xl">⚠️</span>
              <div>
                <h1 className="text-xl font-bold">Application Error</h1>
                <p className="text-red-100 text-sm">Error Code: {errorCode}</p>
              </div>
            </div>
            <div className="text-right text-sm text-red-100">
              <div>Environment: {process.env.NODE_ENV}</div>
              <div>Retry Count: {retryCount}</div>
            </div>
          </div>
        </div>
      </div>

      <div className="max-w-7xl mx-auto px-6 py-8">
        <div className="grid lg:grid-cols-3 gap-8">
          {/* Main Error Information */}
          <div className="lg:col-span-2 space-y-6">
            {/* Error Summary */}
            <div className="bg-slate-800 rounded-lg border border-slate-700 p-6">
              <h2 className="text-xl font-bold text-red-400 mb-4 flex items-center">
                <span className="mr-2">🐛</span>
                Error Summary
              </h2>

              {error ? (
                <div className="space-y-4">
                  <div>
                    <h3 className="text-sm font-semibold text-slate-300 mb-2">
                      Message
                    </h3>
                    <div className="bg-slate-900 border border-slate-600 rounded p-3 font-mono text-sm text-red-300">
                      {error.message || "Unknown error occurred"}
                    </div>
                  </div>

                  {error.digest && (
                    <div>
                      <h3 className="text-sm font-semibold text-slate-300 mb-2">
                        Digest
                      </h3>
                      <div className="bg-slate-900 border border-slate-600 rounded p-3 font-mono text-sm text-yellow-300">
                        {error.digest}
                      </div>
                    </div>
                  )}
                </div>
              ) : (
                <div className="bg-slate-900 border border-slate-600 rounded p-3 text-slate-400">
                  No error object available
                </div>
              )}
            </div>

            {/* Stack Trace */}
            {error?.stack && (
              <div className="bg-slate-800 rounded-lg border border-slate-700 p-6">
                <div className="flex items-center justify-between mb-4">
                  <h2 className="text-xl font-bold text-orange-400 flex items-center">
                    <span className="mr-2">📋</span>
                    Stack Trace
                  </h2>
                  <button
                    onClick={() => setShowFullStack(!showFullStack)}
                    className="text-sm bg-slate-700 text-slate-300 px-3 py-1 rounded hover:bg-slate-600 transition-colors"
                  >
                    {showFullStack ? "Collapse" : "Expand"}
                  </button>
                </div>

                <div className="bg-slate-900 border border-slate-600 rounded p-4 overflow-auto">
                  <pre className="text-xs text-slate-300 whitespace-pre-wrap">
                    {showFullStack
                      ? error.stack
                      : error.stack.split("\n").slice(0, 5).join("\n") +
                        "\n..."}
                  </pre>
                </div>
              </div>
            )}

            {/* Context Information */}
            {errorContext && (
              <div className="bg-slate-800 rounded-lg border border-slate-700 p-6">
                <h2 className="text-xl font-bold text-blue-400 mb-4 flex items-center">
                  <span className="mr-2">🔍</span>
                  Context Information
                </h2>

                <div className="grid md:grid-cols-2 gap-4">
                  {Object.entries(errorContext).map(([key, value]) => (
                    <div key={key}>
                      <h3 className="text-sm font-semibold text-slate-300 mb-1 capitalize">
                        {key.replace(/([A-Z])/g, " $1").trim()}
                      </h3>
                      <div className="bg-slate-900 border border-slate-600 rounded p-2 text-sm text-slate-300 font-mono break-all">
                        {value || "N/A"}
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* Reproduction Steps */}
            <div className="bg-slate-800 rounded-lg border border-slate-700 p-6">
              <h2 className="text-xl font-bold text-green-400 mb-4 flex items-center">
                <span className="mr-2">🔄</span>
                Debugging Actions
              </h2>

              <div className="grid md:grid-cols-2 gap-4">
                <div className="space-y-3">
                  <h3 className="text-sm font-semibold text-slate-300">
                    Quick Actions
                  </h3>
                  {reset && (
                    <button
                      onClick={handleRetry}
                      className="w-full bg-blue-600 text-white px-4 py-3 rounded hover:bg-blue-700 transition-colors font-semibold"
                    >
                      Retry Operation
                    </button>
                  )}
                  <button
                    onClick={() => window.location.reload()}
                    className="w-full bg-slate-600 text-white px-4 py-3 rounded hover:bg-slate-500 transition-colors font-semibold"
                  >
                    Hard Refresh
                  </button>
                  <Link
                    href="/"
                    className="block w-full bg-green-600 text-white px-4 py-3 rounded hover:bg-green-700 transition-colors font-semibold text-center"
                  >
                    Return Home
                  </Link>
                </div>

                <div className="space-y-3">
                  <h3 className="text-sm font-semibold text-slate-300">
                    Report Actions
                  </h3>
                  <button
                    onClick={copyErrorReport}
                    className="w-full bg-purple-600 text-white px-4 py-3 rounded hover:bg-purple-700 transition-colors font-semibold"
                  >
                    {copied ? "✓ Copied!" : "Copy Error Report"}
                  </button>
                  <Link
                    href="/support/bug-report"
                    className="block w-full bg-orange-600 text-white px-4 py-3 rounded hover:bg-orange-700 transition-colors font-semibold text-center"
                  >
                    File Bug Report
                  </Link>
                  <a
                    href={`mailto:dev@example.com?subject=Error Report - ${errorCode}&body=${encodeURIComponent(`Error Code: ${errorCode}\nURL: ${window.location.href}\nMessage: ${error?.message}`)}`}
                    className="block w-full bg-red-600 text-white px-4 py-3 rounded hover:bg-red-700 transition-colors font-semibold text-center"
                  >
                    Email Developers
                  </a>
                </div>
              </div>
            </div>
          </div>

          {/* Sidebar */}
          <div className="space-y-6">
            {/* Error Statistics */}
            <div className="bg-slate-800 rounded-lg border border-slate-700 p-6">
              <h3 className="text-lg font-bold text-slate-200 mb-4">
                Error Details
              </h3>
              <div className="space-y-3 text-sm">
                <div className="flex justify-between">
                  <span className="text-slate-400">Severity:</span>
                  <span className="text-red-400 font-semibold">High</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-slate-400">Type:</span>
                  <span className="text-yellow-400">Runtime Error</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-slate-400">Retries:</span>
                  <span className="text-blue-400">{retryCount}</span>
                </div>
                <div className="flex justify-between">
                  <span className="text-slate-400">Timestamp:</span>
                  <span className="text-slate-300 font-mono text-xs">
                    {errorContext?.timestamp.split("T")[1]?.split(".")[0]}
                  </span>
                </div>
              </div>
            </div>

            {/* Developer Resources */}
            <div className="bg-slate-800 rounded-lg border border-slate-700 p-6">
              <h3 className="text-lg font-bold text-slate-200 mb-4">
                Developer Resources
              </h3>
              <div className="space-y-2">
                <Link
                  href="/docs/error-handling"
                  className="block text-blue-400 hover:text-blue-300 text-sm"
                >
                  📚 Error Handling Guide
                </Link>
                <Link
                  href="/docs/debugging"
                  className="block text-blue-400 hover:text-blue-300 text-sm"
                >
                  🔧 Debugging Documentation
                </Link>
                <Link
                  href="/api/health"
                  className="block text-blue-400 hover:text-blue-300 text-sm"
                >
                  🏥 Health Check API
                </Link>
                <a
                  href="https://github.com/yourrepo/issues/new"
                  target="_blank"
                  rel="noopener noreferrer"
                  className="block text-blue-400 hover:text-blue-300 text-sm"
                >
                  🐛 Report on GitHub
                </a>
              </div>
            </div>

            {/* Environment Info */}
            <div className="bg-slate-800 rounded-lg border border-slate-700 p-6">
              <h3 className="text-lg font-bold text-slate-200 mb-4">
                Environment
              </h3>
              <div className="space-y-2 text-sm">
                <div className="flex justify-between">
                  <span className="text-slate-400">Mode:</span>
                  <span
                    className={`font-semibold ${
                      process.env.NODE_ENV === "development"
                        ? "text-yellow-400"
                        : "text-green-400"
                    }`}
                  >
                    {process.env.NODE_ENV}
                  </span>
                </div>
                <div className="flex justify-between">
                  <span className="text-slate-400">Build:</span>
                  <span className="text-slate-300 font-mono text-xs">
                    {process.env.NEXT_PUBLIC_APP_VERSION || "unknown"}
                  </span>
                </div>
                <div className="flex justify-between">
                  <span className="text-slate-400">Browser:</span>
                  <span className="text-slate-300 text-xs">
                    {navigator.userAgent.split(" ")[0]}
                  </span>
                </div>
              </div>
            </div>

            {/* System Status */}
            <div className="bg-slate-800 rounded-lg border border-slate-700 p-6">
              <h3 className="text-lg font-bold text-slate-200 mb-4">
                System Status
              </h3>
              <div className="space-y-2">
                <div className="flex items-center space-x-2">
                  <div className="w-2 h-2 bg-green-500 rounded-full"></div>
                  <span className="text-sm text-slate-300">API Services</span>
                </div>
                <div className="flex items-center space-x-2">
                  <div className="w-2 h-2 bg-yellow-500 rounded-full"></div>
                  <span className="text-sm text-slate-300">Frontend App</span>
                </div>
                <div className="flex items-center space-x-2">
                  <div className="w-2 h-2 bg-green-500 rounded-full"></div>
                  <span className="text-sm text-slate-300">Database</span>
                </div>
                <Link
                  href="/status"
                  className="block text-blue-400 hover:text-blue-300 text-sm mt-3"
                >
                  View Full Status →
                </Link>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
```

#### Customization Options

- Error tracking: Integrate with monitoring services like Sentry, DataDog, or custom logging
- Context collection: Add more environment-specific information (user ID, feature flags, etc.)
- Debugging tools: Add buttons to toggle debug mode, clear cache, or reset state
- Developer workflows: Connect to your issue tracking system (GitHub, Jira, etc.)
- Environment detection: Show different information based on development vs production
- Health checks: Display real-time system status and health metrics

<BackToTop />

### Example #5: Accessible Error Page with Multiple Languages

An inclusive error page that prioritizes accessibility and supports multiple languages for international users.

```tsx
import React, { useState, useEffect } from "react";
import Link from "next/link";

interface ErrorPageProps {
  error?: Error & { digest?: string };
  reset?: () => void;
}

interface Translation {
  title: string;
  subtitle: string;
  description: string;
  tryAgain: string;
  goHome: string;
  contactSupport: string;
  reportError: string;
  errorCode: string;
  whatHappened: string;
  howToFix: string;
  needHelp: string;
  accessibilityNote: string;
  skipToContent: string;
  mainContent: string;
  supportInfo: string;
}

const translations: Record<string, Translation> = {
  en: {
    title: "An Error Occurred",
    subtitle: "We're sorry for the inconvenience",
    description:
      "Something unexpected happened while processing your request. Our team has been notified and is working to resolve this issue.",
    tryAgain: "Try Again",
    goHome: "Go to Homepage",
    contactSupport: "Contact Support",
    reportError: "Report This Error",
    errorCode: "Error Code",
    whatHappened: "What happened?",
    howToFix: "How to fix this:",
    needHelp: "Need additional help?",
    accessibilityNote:
      "For screen reader users: This page contains error information and recovery options.",
    skipToContent: "Skip to main content",
    mainContent: "Main content",
    supportInfo: "Our support team is available 24/7 to assist you",
  },
  es: {
    title: "Ha Ocurrido un Error",
    subtitle: "Lamentamos las molestias",
    description:
      "Algo inesperado ocurrió mientras procesábamos su solicitud. Nuestro equipo ha sido notificado y está trabajando para resolver este problema.",
    tryAgain: "Intentar de Nuevo",
    goHome: "Ir al Inicio",
    contactSupport: "Contactar Soporte",
    reportError: "Reportar Este Error",
    errorCode: "Código de Error",
    whatHappened: "¿Qué pasó?",
    howToFix: "Cómo solucionarlo:",
    needHelp: "¿Necesita ayuda adicional?",
    accessibilityNote:
      "Para usuarios de lectores de pantalla: Esta página contiene información de error y opciones de recuperación.",
    skipToContent: "Saltar al contenido principal",
    mainContent: "Contenido principal",
    supportInfo: "Nuestro equipo de soporte está disponible 24/7 para ayudarle",
  },
  fr: {
    title: "Une Erreur s'est Produite",
    subtitle: "Nous sommes désolés pour la gêne occasionnée",
    description:
      "Quelque chose d'inattendu s'est produit lors du traitement de votre demande. Notre équipe a été notifiée et travaille à résoudre ce problème.",
    tryAgain: "Réessayer",
    goHome: "Aller à l'Accueil",
    contactSupport: "Contacter le Support",
    reportError: "Signaler Cette Erreur",
    errorCode: "Code d'Erreur",
    whatHappened: "Que s'est-il passé ?",
    howToFix: "Comment résoudre ceci :",
    needHelp: "Besoin d'aide supplémentaire ?",
    accessibilityNote:
      "Pour les utilisateurs de lecteurs d'écran : Cette page contient des informations d'erreur et des options de récupération.",
    skipToContent: "Passer au contenu principal",
    mainContent: "Contenu principal",
    supportInfo:
      "Notre équipe de support est disponible 24h/24 et 7j/7 pour vous aider",
  },
};

export default function AccessibleError({ error, reset }: ErrorPageProps) {
  const [language, setLanguage] = useState<string>("en");
  const [highContrast, setHighContrast] = useState(false);
  const [fontSize, setFontSize] = useState<"normal" | "large" | "larger">(
    "normal",
  );
  const [announceError, setAnnounceError] = useState(false);

  const t = translations[language] || translations.en;
  const errorCode =
    error?.digest || `ERR_${Date.now().toString(36).toUpperCase()}`;

  useEffect(() => {
    // Detect user's preferred language
    const userLanguage = navigator.language.split("-")[0];
    if (translations[userLanguage]) {
      setLanguage(userLanguage);
    }

    // Check for accessibility preferences
    const prefersHighContrast = window.matchMedia(
      "(prefers-contrast: high)",
    ).matches;
    const prefersReducedMotion = window.matchMedia(
      "(prefers-reduced-motion: reduce)",
    ).matches;

    setHighContrast(prefersHighContrast);

    // Announce error to screen readers
    setAnnounceError(true);

    // Set page title for screen readers
    document.title = `${t.title} - ${errorCode}`;
  }, [language, t.title]);

  const fontSizeClasses = {
    normal: "text-base",
    large: "text-lg",
    larger: "text-xl",
  };

  const themeClasses = highContrast
    ? "bg-black text-white"
    : "bg-gray-50 text-gray-900";

  return (
    <div
      className={`min-h-screen ${themeClasses} ${fontSizeClasses[fontSize]}`}
    >
      {/* Skip Link for Keyboard Navigation */}
      <a
        href="#main-content"
        className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-blue-600 text-white px-4 py-2 rounded-lg z-50"
      >
        {t.skipToContent}
      </a>

      {/* Accessibility Controls */}
      <div className="bg-gray-800 text-white p-2">
        <div className="max-w-4xl mx-auto flex flex-wrap items-center justify-between gap-4 text-sm">
          {/* Language Selector */}
          <div className="flex items-center space-x-2">
            <label htmlFor="language-select" className="font-semibold">
              Language:
            </label>
            <select
              id="language-select"
              value={language}
              onChange={(e) => setLanguage(e.target.value)}
              className="bg-gray-700 text-white px-2 py-1 rounded border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
            >
              <option value="en">English</option>
              <option value="es">Español</option>
              <option value="fr">Français</option>
            </select>
          </div>

          {/* Accessibility Controls */}
          <div className="flex items-center space-x-4">
            <button
              onClick={() => setHighContrast(!highContrast)}
              className="px-3 py-1 rounded bg-gray-700 hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
              aria-label="Toggle high contrast mode"
            >
              {highContrast ? "🌞" : "🌙"} Contrast
            </button>

            <div className="flex items-center space-x-1">
              <button
                onClick={() => setFontSize("normal")}
                className={`px-2 py-1 rounded text-sm ${fontSize === "normal" ? "bg-blue-600" : "bg-gray-700 hover:bg-gray-600"} focus:outline-none focus:ring-2 focus:ring-blue-500`}
                aria-label="Normal font size"
              >
                A
              </button>
              <button
                onClick={() => setFontSize("large")}
                className={`px-2 py-1 rounded text-base ${fontSize === "large" ? "bg-blue-600" : "bg-gray-700 hover:bg-gray-600"} focus:outline-none focus:ring-2 focus:ring-blue-500`}
                aria-label="Large font size"
              >
                A
              </button>
              <button
                onClick={() => setFontSize("larger")}
                className={`px-2 py-1 rounded text-lg ${fontSize === "larger" ? "bg-blue-600" : "bg-gray-700 hover:bg-gray-600"} focus:outline-none focus:ring-2 focus:ring-blue-500`}
                aria-label="Larger font size"
              >
                A
              </button>
            </div>
          </div>
        </div>
      </div>

      {/* Live Region for Screen Reader Announcements */}
      <div aria-live="polite" aria-atomic="true" className="sr-only">
        {announceError && `${t.accessibilityNote} ${t.errorCode}: ${errorCode}`}
      </div>

      {/* Main Content */}
      <main id="main-content" className="max-w-4xl mx-auto px-6 py-12">
        <div className="text-center mb-12">
          {/* Error Icon with Alt Text */}
          <div
            className={`w-24 h-24 mx-auto mb-6 rounded-full flex items-center justify-center ${
              highContrast ? "bg-white text-black" : "bg-red-100 text-red-600"
            }`}
            role="img"
            aria-label="Error icon"
          >
            <span className="text-4xl" aria-hidden="true">
              ⚠️
            </span>
          </div>

          {/* Main Error Message */}
          <h1 className="text-3xl font-bold mb-4">{t.title}</h1>
          <p className="text-xl mb-6 text-gray-600">{t.subtitle}</p>
          <p className="text-lg max-w-2xl mx-auto">{t.description}</p>
        </div>

        <div className="grid lg:grid-cols-2 gap-8">
          {/* Error Information */}
          <section
            className={`rounded-xl p-6 ${
              highContrast
                ? "bg-gray-900 border border-white"
                : "bg-white shadow-lg border border-gray-200"
            }`}
          >
            <h2 className="text-xl font-bold mb-4">{t.whatHappened}</h2>

            <div className="space-y-4">
              <div>
                <h3 className="font-semibold mb-2">{t.errorCode}:</h3>
                <code
                  className={`block p-3 rounded font-mono text-sm ${
                    highContrast
                      ? "bg-black border border-gray-600"
                      : "bg-gray-100 border border-gray-300"
                  }`}
                >
                  {errorCode}
                </code>
              </div>

              {error?.message && (
                <div>
                  <h3 className="font-semibold mb-2">Technical Details:</h3>
                  <div
                    className={`p-3 rounded text-sm ${
                      highContrast
                        ? "bg-black border border-gray-600"
                        : "bg-gray-100 border border-gray-300"
                    }`}
                  >
                    {error.message}
                  </div>
                </div>
              )}
            </div>
          </section>

          {/* Recovery Actions */}
          <section
            className={`rounded-xl p-6 ${
              highContrast
                ? "bg-gray-900 border border-white"
                : "bg-white shadow-lg border border-gray-200"
            }`}
          >
            <h2 className="text-xl font-bold mb-4">{t.howToFix}</h2>

            <div className="space-y-4">
              {reset && (
                <button
                  onClick={reset}
                  className="w-full bg-blue-600 text-white px-6 py-4 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-4 focus:ring-blue-300 transition-colors font-semibold"
                  aria-describedby="try-again-desc"
                >
                  {t.tryAgain}
                </button>
              )}
              <p id="try-again-desc" className="text-sm text-gray-600 sr-only">
                Attempts to reload the current page and retry the failed
                operation
              </p>

              <Link
                href="/"
                className={`block w-full text-center px-6 py-4 rounded-lg transition-colors font-semibold focus:outline-none focus:ring-4 ${
                  highContrast
                    ? "bg-white text-black hover:bg-gray-200 focus:ring-gray-300"
                    : "bg-gray-100 text-gray-700 hover:bg-gray-200 focus:ring-gray-300"
                }`}
                aria-describedby="go-home-desc"
              >
                {t.goHome}
              </Link>
              <p id="go-home-desc" className="text-sm text-gray-600 sr-only">
                Returns you to the main homepage of the website
              </p>

              <Link
                href="/contact"
                className={`block w-full text-center px-6 py-4 rounded-lg transition-colors font-semibold focus:outline-none focus:ring-4 ${
                  highContrast
                    ? "border border-white hover:bg-gray-800 focus:ring-gray-300"
                    : "border border-gray-300 hover:bg-gray-50 focus:ring-gray-300"
                }`}
                aria-describedby="contact-desc"
              >
                {t.contactSupport}
              </Link>
              <p id="contact-desc" className="text-sm text-gray-600 sr-only">
                Opens the contact page where you can reach our support team
              </p>
            </div>
          </section>
        </div>

        {/* Support Information */}
        <section
          className={`mt-12 text-center p-6 rounded-xl ${
            highContrast
              ? "bg-gray-900 border border-white"
              : "bg-blue-50 border border-blue-200"
          }`}
        >
          <h2 className="text-xl font-bold mb-4">{t.needHelp}</h2>
          <p className="mb-6">{t.supportInfo}</p>

          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href="mailto:support@example.com"
              className="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-4 focus:ring-blue-300 transition-colors font-semibold"
            >
              📧 support@example.com
            </a>
            <a
              href="tel:+1-555-123-4567"
              className="bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 focus:outline-none focus:ring-4 focus:ring-green-300 transition-colors font-semibold"
            >
              📞 +1 (555) 123-4567
            </a>
          </div>

          {/* Additional Accessibility Info */}
          <div className="mt-6 pt-6 border-t border-gray-300">
            <p className="text-sm text-gray-600">
              <strong>Accessibility:</strong> If you're having trouble using
              this page with assistive technology, please contact us directly at
              accessibility@example.com or call our accessibility hotline at +1
              (555) 123-4568.
            </p>
          </div>
        </section>
      </main>
    </div>
  );
}
```

#### Customization Options

- Languages: Add more languages by extending the translations object
- Accessibility features: Add more accessibility controls (motion preferences, color blind support)
- Screen reader support: Enhance ARIA labels and live regions for better screen reader experience
- Keyboard navigation: Improve focus management and keyboard shortcuts
- High contrast themes: Create multiple high contrast color schemes
- Font options: Add more font size options and font family choices
- Voice commands: Integrate with speech recognition APIs for voice navigation

<BackToTop />
