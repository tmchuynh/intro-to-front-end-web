import BackToTop from "@/components/BackToTop";

# Project Details Page

## Table of Contents

## About This Page

This page is designed to showcase the details of a specific project. It includes a header with the project title, a description, and a list of technologies used in the project. The page also features a link to the project's GitHub repository and a button to view the live project.

This will be the page where users can see the details of a specific project. It will be linked from the portfolio page and will serve as a detailed view of the project.

This page will be dynamically generated based on the project ID passed in the URL. This allows you to create a unique page for each project without having to create separate files for each one.

## Dynamic Routing

To create a dynamic project details page, you will need to use Next.js's file-based routing system. You will create a folder named `[projectId]` inside the `portfolio` directory. Within that folder, you will create a `page.tsx` file that fetches and displays the details of the project based on the `projectId` parameter in the URL.

The file structure would look like this:

```
portfolio/
  page.tsx          // Main portfolio page
  [projectId]/
    page.tsx      // Dynamic project detail page
```

## What You Should Include

Since you will want to display the details of each project, you should include the following:

- **Title**: The name of the project.
- **Description**: A detailed overview of what the project is about.
- **Technologies Used**: A list of technologies or tools used in the project.
- **GitHub Link**: A link to the project's GitHub repository.
- **Live Project Link**: A link to view the live project.
- **Image**: A thumbnail or screenshot of the project.
- **Date**: The date when the project was completed or published.
- **Category**: The category or type of project (e.g., web development, mobile app, etc.).

##### TIP

<blockquote>
  You can use the same data structure you created for the portfolio page to
  fetch the project details. This way, you can reuse the same data and avoid
  duplicating information. You can fetch the project details based on the
  `projectId` parameter in the URL and display the relevant information on the
  project details page.
</blockquote>

<BackToTop />

## Important Considerations

- **Responsive Design**: Ensure the page is mobile-friendly and looks good on all devices.
- **Accessibility**: Make sure the page is accessible to all users, including those with disabilities. Use semantic HTML, provide alt text for images, and ensure keyboard navigation works.
- **Dark Mode**: Consider implementing a dark mode toggle for users who prefer a darker interface.
- **Performance**: Optimize images and assets to ensure fast loading times. Use lazy loading for images if necessary.
- **SEO Optimization**: Use appropriate meta tags, titles, and descriptions to improve search engine visibility.
- **Testing**: Test the page on different devices and browsers to ensure compatibility and functionality.
- **Analytics**: Consider adding analytics tracking to monitor user interactions with the project details page, such as clicks on links or buttons.
- **User Feedback**: Consider adding a way for users to provide feedback on the project, such as a comment section or a rating system.
  <BackToTop />

## Optional Features

- **Related Projects**: Display a list of related projects at the bottom of the page to encourage users to explore more of your work.
- **Project Timeline**: Include a timeline of the project's development process, showcasing key milestones and achievements.
- **Video Demo**: If applicable, include a video demo of the project to give users a better understanding of how it works.
- **Downloadable Assets**: Provide downloadable assets related to the project, such as design files, documentation, or source code.
- **Social Sharing**: Add social media sharing buttons to allow users to share the project details on their social networks.
- **Contact Information**: Include a way for users to contact you for inquiries or collaborations related to the project.
- **Project Updates**: If the project is ongoing, consider adding a section for updates or future plans for the project, keeping users informed about its progress.
- **Testimonials**: If applicable, include testimonials or feedback from users or clients who have interacted with the project, adding credibility and social proof.
- **Downloadable Resume**: If the project is part of your portfolio, consider including a downloadable resume or CV that highlights your skills and experience related to the project.
- **Call to Action**: Encourage users to take action, such as following you on social media, subscribing to your newsletter, or contacting you for potential collaborations.
- **Project Challenges**: Share any challenges you faced during the project and how you overcame them. This can provide insight into your problem-solving skills and resilience.
- **Future Plans**: If you have plans to update or expand the project in the future, consider including a section that outlines your vision for the project's evolution. This can help keep users engaged and interested in your work.
- **Project Impact**: If applicable, discuss the impact of the project, such as how it has helped users, solved a problem, or contributed to a community. This can add depth to the project details and showcase its significance.
- **Project Roadmap**: If the project has a roadmap for future development, consider including a section that outlines the planned features or improvements. This can give users insight into the project's direction and encourage them to stay engaged.
  <BackToTop />

## Examples


### Example 1: Minimal Project Details Page (Production-Ready)

- Displays the project title, description, technologies used, GitHub link, live project link, and an image in a clean, accessible layout.
- Includes a prominent button to view the live project and a styled link to the GitHub repository.
- Responsive, accessible, and ready for real-world use.

```tsx
import React from "react";

interface ProjectDetailsProps {
  title: string;
  description: string;
  technologies: string[];
  githubUrl: string;
  liveUrl: string;
  imageUrl: string;
  date: string;
  category: string;
}

const MinimalProjectDetails: React.FC<ProjectDetailsProps> = ({
  title,
  description,
  technologies,
  githubUrl,
  liveUrl,
  imageUrl,
  date,
  category,
}) => (
  <div className="max-w-3xl mx-auto p-6 bg-white rounded-lg shadow-md">
    <img src={imageUrl} alt={title} className="w-full h-64 object-cover rounded-md mb-6" />
    <h1 className="text-3xl font-bold mb-2">{title}</h1>
    <p className="text-gray-600 mb-4">{description}</p>
    <div className="flex flex-wrap gap-2 mb-4">
      {technologies.map((tech) => (
        <span key={tech} className="bg-blue-100 text-blue-700 px-2 py-1 rounded text-xs font-medium">
          {tech}
        </span>
      ))}
    </div>
    <div className="flex items-center gap-4 mb-4">
      <a href={githubUrl} target="_blank" rel="noopener noreferrer" className="text-blue-600 hover:underline flex items-center gap-1">
        <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M12 .5C5.73.5.5 5.73.5 12c0 5.08 3.29 9.39 7.86 10.91.58.11.79-.25.79-.56 0-.28-.01-1.02-.02-2-3.2.7-3.88-1.54-3.88-1.54-.53-1.34-1.3-1.7-1.3-1.7-1.06-.72.08-.71.08-.71 1.17.08 1.79 1.2 1.79 1.2 1.04 1.78 2.73 1.27 3.4.97.11-.75.41-1.27.74-1.56-2.55-.29-5.23-1.28-5.23-5.7 0-1.26.45-2.29 1.19-3.1-.12-.29-.52-1.46.11-3.05 0 0 .97-.31 3.18 1.18a11.1 11.1 0 012.9-.39c.98.01 1.97.13 2.9.39 2.2-1.49 3.17-1.18 3.17-1.18.63 1.59.23 2.76.11 3.05.74.81 1.19 1.84 1.19 3.1 0 4.43-2.69 5.41-5.25 5.7.42.36.79 1.09.79 2.2 0 1.59-.01 2.87-.01 3.26 0 .31.21.67.8.56C20.71 21.39 24 17.08 24 12c0-6.27-5.23-11.5-12-11.5z"/></svg>
        GitHub
      </a>
      <a href={liveUrl} target="_blank" rel="noopener noreferrer" className="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 font-semibold transition">View Live</a>
    </div>
    <div className="flex items-center gap-4 text-sm text-gray-500">
      <span>{date}</span>
      <span>•</span>
      <span>{category}</span>
    </div>
  </div>
);
export default MinimalProjectDetails;
```

##### Customization Options
- Change the layout, colors, and typography to match your brand
- Add animations or transitions for image and buttons
- Replace the image with a video or carousel
- Add a back button to return to the portfolio
- Make the technologies list collapsible for long lists
- Add tooltips for technologies
- Integrate analytics for button clicks
- Add a loading skeleton for data fetching
- Make the card style more compact or more detailed
- Add a print or download button for project details

<BackToTop />


### Example 2: Project Details Page with Related Projects

- All features of the minimal details page, plus a section for related projects at the bottom.
- Related projects are fetched from the same data source and displayed as cards with titles, images, and links.
- Responsive grid layout for related projects.

```tsx
import React from "react";

interface Project {
  id: string;
  title: string;
  imageUrl: string;
  description: string;
}

interface ProjectDetailsWithRelatedProps {
  // ...all props from MinimalProjectDetails
  relatedProjects: Project[];
}

const ProjectDetailsWithRelated: React.FC<ProjectDetailsWithRelatedProps> = (props) => (
  <div>
    {/* ...MinimalProjectDetails code here... */}
    <section className="mt-12">
      <h2 className="text-2xl font-bold mb-4">Related Projects</h2>
      <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
        {props.relatedProjects.map((project) => (
          <a key={project.id} href={`/portfolio/${project.id}`} className="block bg-white rounded-lg shadow hover:shadow-lg transition p-4">
            <img src={project.imageUrl} alt={project.title} className="w-full h-32 object-cover rounded mb-2" />
            <h3 className="font-semibold text-lg mb-1">{project.title}</h3>
            <p className="text-gray-500 text-sm line-clamp-2">{project.description}</p>
          </a>
        ))}
      </div>
    </section>
  </div>
);
export default ProjectDetailsWithRelated;
```

##### Customization Options
- Change the number of columns in the grid
- Add a "See All Projects" button below the related projects
- Show only projects from the same category or with shared tags
- Add hover animations to project cards
- Display project tags or categories on each card
- Add a carousel for related projects on mobile
- Show a loading skeleton while fetching related projects
- Add analytics for related project clicks
- Make the related projects section collapsible

<BackToTop />


### Example 3: Project Details with Downloadable Assets & Social Sharing

- All features of the previous examples, plus a section for downloadable assets and social sharing buttons.
- Downloadable assets can include design files, documentation, or source code.
- Social sharing buttons for Twitter, LinkedIn, and Facebook.

```tsx
import React from "react";

interface DownloadableAsset {
  name: string;
  url: string;
}

interface ProjectDetailsWithAssetsProps {
  // ...all props from ProjectDetailsWithRelated
  assets: DownloadableAsset[];
}

const ProjectDetailsWithAssets: React.FC<ProjectDetailsWithAssetsProps> = (props) => (
  <div>
    {/* ...ProjectDetailsWithRelated code here... */}
    <section className="mt-12">
      <h2 className="text-xl font-bold mb-3">Downloadable Assets</h2>
      <ul className="space-y-2">
        {props.assets.map((asset) => (
          <li key={asset.name}>
            <a href={asset.url} download className="text-blue-600 hover:underline flex items-center gap-1">
              <svg className="w-4 h-4" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24"><path d="M12 4v12m0 0l-4-4m4 4l4-4m-4 4V4"/></svg>
              {asset.name}
            </a>
          </li>
        ))}
      </ul>
    </section>
    <section className="mt-8">
      <h2 className="text-xl font-bold mb-3">Share This Project</h2>
      <div className="flex gap-4">
        <a href="#" aria-label="Share on Twitter" className="text-blue-400 hover:text-blue-600">
          <svg className="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M23 3a10.9 10.9 0 01-3.14 1.53A4.48 4.48 0 0022.4.36a9.09 9.09 0 01-2.88 1.1A4.52 4.52 0 0016.11 0c-2.5 0-4.52 2.02-4.52 4.52 0 .35.04.7.11 1.03C7.69 5.4 4.07 3.6 1.64.96c-.38.65-.6 1.4-.6 2.2 0 1.52.77 2.86 1.95 3.65A4.48 4.48 0 01.96 5.1v.06c0 2.13 1.52 3.91 3.54 4.31-.37.1-.76.16-1.16.16-.28 0-.55-.03-.81-.08.55 1.7 2.16 2.94 4.07 2.97A9.05 9.05 0 010 21.54a12.8 12.8 0 006.92 2.03c8.3 0 12.85-6.88 12.85-12.85 0-.2 0-.39-.01-.58A9.22 9.22 0 0023 3z"/></svg>
        </a>
        <a href="#" aria-label="Share on LinkedIn" className="text-blue-700 hover:text-blue-900">
          <svg className="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M19 0h-14c-2.76 0-5 2.24-5 5v14c0 2.76 2.24 5 5 5h14c2.76 0 5-2.24 5-5v-14c0-2.76-2.24-5-5-5zm-11 19h-3v-9h3v9zm-1.5-10.28c-.97 0-1.75-.79-1.75-1.75s.78-1.75 1.75-1.75 1.75.78 1.75 1.75-.78 1.75-1.75 1.75zm13.5 10.28h-3v-4.5c0-1.08-.02-2.47-1.5-2.47-1.5 0-1.73 1.17-1.73 2.39v4.58h-3v-9h2.89v1.23h.04c.4-.75 1.38-1.54 2.84-1.54 3.04 0 3.6 2 3.6 4.59v4.72z"/></svg>
        </a>
        <a href="#" aria-label="Share on Facebook" className="text-blue-600 hover:text-blue-800">
          <svg className="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M22.675 0h-21.35C.595 0 0 .592 0 1.326v21.348C0 23.408.595 24 1.326 24H12.82v-9.294H9.692v-3.622h3.127V8.413c0-3.1 1.893-4.788 4.659-4.788 1.325 0 2.463.099 2.797.143v3.24l-1.918.001c-1.504 0-1.797.715-1.797 1.763v2.313h3.587l-.467 3.622h-3.12V24h6.116C23.406 24 24 23.408 24 22.674V1.326C24 .592 23.406 0 22.675 0z"/></svg>
        </a>
      </div>
    </section>
  </div>
);
export default ProjectDetailsWithAssets;
```

##### Customization Options
- Add more social networks or custom share links
- Show download counts for each asset
- Add preview thumbnails for assets
- Display a QR code for sharing the project
- Add copy-to-clipboard for project links
- Show a toast notification after download/share
- Make the assets section collapsible
- Add analytics for downloads and shares
- Style the social buttons with brand colors

<BackToTop />


### Example 4: Project Details with Timeline, Updates, Impact, Challenges, and Future Plans

- All features of the previous examples, plus a timeline of project milestones, updates, impact, challenges, and future plans.
- Timeline is displayed as a vertical list with dates and descriptions.
- Updates, impact, challenges, and future plans are shown in dedicated sections.

```tsx
import React from "react";

interface TimelineEvent {
  date: string;
  description: string;
}

interface ProjectDetailsWithTimelineProps {
  // ...all props from ProjectDetailsWithAssets
  timeline: TimelineEvent[];
  updates: string[];
  impact: string;
  challenges: string[];
  futurePlans: string;
}

const ProjectDetailsWithTimeline: React.FC<ProjectDetailsWithTimelineProps> = (props) => (
  <div>
    {/* ...ProjectDetailsWithAssets code here... */}
    <section className="mt-12">
      <h2 className="text-xl font-bold mb-3">Project Timeline</h2>
      <ol className="border-l-2 border-blue-200 pl-6 space-y-4">
        {props.timeline.map((event, i) => (
          <li key={i} className="relative">
            <span className="absolute -left-3 top-1 w-3 h-3 bg-blue-400 rounded-full"></span>
            <div className="text-sm text-gray-500 mb-1">{event.date}</div>
            <div className="text-base text-gray-700">{event.description}</div>
          </li>
        ))}
      </ol>
    </section>
    <section className="mt-8">
      <h2 className="text-xl font-bold mb-3">Project Updates</h2>
      <ul className="list-disc pl-6 space-y-1 text-gray-700">
        {props.updates.map((update, i) => (
          <li key={i}>{update}</li>
        ))}
      </ul>
    </section>
    <section className="mt-8">
      <h2 className="text-xl font-bold mb-3">Project Impact</h2>
      <p className="text-gray-700">{props.impact}</p>
    </section>
    <section className="mt-8">
      <h2 className="text-xl font-bold mb-3">Project Challenges</h2>
      <ul className="list-disc pl-6 space-y-1 text-gray-700">
        {props.challenges.map((challenge, i) => (
          <li key={i}>{challenge}</li>
        ))}
      </ul>
    </section>
    <section className="mt-8">
      <h2 className="text-xl font-bold mb-3">Future Plans</h2>
      <p className="text-gray-700">{props.futurePlans}</p>
    </section>
  </div>
);
export default ProjectDetailsWithTimeline;
```

##### Customization Options
- Add icons or images to timeline events
- Make the timeline horizontal on desktop
- Add animations to timeline entries
- Show update dates and authors
- Display impact metrics (users, downloads, etc.)
- Add collapsible sections for challenges and plans
- Integrate a roadmap visualization
- Add a feedback form for future plans
- Show a progress bar for project completion

<BackToTop />


### Example 5: Project Details with Testimonials, Contact, and Call to Action

- All features of the previous examples, plus testimonials, contact information, and a call to action.
- Testimonials are displayed in styled cards with author names and feedback.
- Contact section includes email, social links, and a contact button.
- Call to action encourages users to follow, subscribe, or collaborate.
- Downloadable resume or CV included.

```tsx
import React from "react";

interface Testimonial {
  author: string;
  feedback: string;
}

interface ProjectDetailsWithTestimonialsProps {
  // ...all props from ProjectDetailsWithTimeline
  testimonials: Testimonial[];
  contactEmail: string;
  socialLinks: { name: string; url: string }[];
  resumeUrl: string;
}

const ProjectDetailsWithTestimonials: React.FC<ProjectDetailsWithTestimonialsProps> = (props) => (
  <div>
    {/* ...ProjectDetailsWithTimeline code here... */}
    <section className="mt-12">
      <h2 className="text-xl font-bold mb-3">Testimonials</h2>
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
        {props.testimonials.map((t, i) => (
          <div key={i} className="bg-gray-50 border border-gray-200 rounded-lg p-4 shadow">
            <p className="text-gray-700 italic mb-2">"{t.feedback}"</p>
            <div className="text-right text-sm text-gray-500">— {t.author}</div>
          </div>
        ))}
      </div>
    </section>
    <section className="mt-8">
      <h2 className="text-xl font-bold mb-3">Contact</h2>
      <p className="mb-2">For inquiries or collaborations, email: <a href={`mailto:${props.contactEmail}`} className="text-blue-600 hover:underline">{props.contactEmail}</a></p>
      <div className="flex gap-4 mb-2">
        {props.socialLinks.map((link) => (
          <a key={link.name} href={link.url} target="_blank" rel="noopener noreferrer" className="text-blue-600 hover:underline">
            {link.name}
          </a>
        ))}
      </div>
      <a href={props.resumeUrl} download className="inline-block mt-2 bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 font-semibold transition">Download Resume</a>
    </section>
    <section className="mt-8">
      <h2 className="text-xl font-bold mb-3">Get Involved</h2>
      <p>Follow me on social media, subscribe to my newsletter, or reach out for potential collaborations!</p>
    </section>
  </div>
);
export default ProjectDetailsWithTestimonials;
```

##### Customization Options
- Add avatars or photos to testimonials
- Integrate a contact form instead of just an email
- Add more social networks or a newsletter signup
- Style the call to action with icons or animations
- Show a map for location-based projects
- Add a WhatsApp or Telegram contact button
- Make testimonials a carousel on mobile
- Add a feedback form for new testimonials
- Track downloads of the resume

<BackToTop />

### Added Downloadable Resume, Contact Information, Testimonials, and Call to Action

- Displays the project title, description, technologies used, GitHub link, live project link, and an image.
- Includes a button to view the live project and a link to the GitHub repository.
- Provides a downloadable resume or CV that highlights skills and experience related to the project.
- Features contact information for inquiries or collaborations related to the project.
- Displays testimonials or feedback from users or clients who have interacted with the project, adding credibility and social proof.
- Encourages users to take action, such as following on social media or subscribing to a newsletter.

<BackToTop />
