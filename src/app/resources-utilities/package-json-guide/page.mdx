import BackToTop from "@/components/BackToTop";

# package.json

## Table of Contents

## Helpful Scripts

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "format": "prettier --write .",
    "test": "jest"
  }
}
```

#### Use Cases

- **`dev`**: Starts the Next.js development server for local development.
- **`build`**: Builds the application for production, optimizing performance and generating static files.
- **`start`**: Starts the production server after building the application.
- **`lint`**: Runs ESLint to check for code quality and style issues.
- **`format`**: Formats the codebase using Prettier, ensuring consistent code style.
- **`test`**: Runs tests using Jest, ensuring code functionality and reliability.
  <BackToTop />

## Dependencies

```json
{
  "dependencies": {
    "next": "^13.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
```

| Dependency  | Description                                                          |
| ----------- | -------------------------------------------------------------------- |
| `next`      | The Next.js framework for building React applications.               |
| `react`     | The React library for building user interfaces.                      |
| `react-dom` | The React DOM library for rendering React components in the browser. |

## devDependencies

```json
{
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0",
    "jest": "^27.0.0"
  }
}
```

| devDependency | Description                                                            |
| ------------- | ---------------------------------------------------------------------- |
| `eslint`      | A tool for identifying and fixing problems in JavaScript code.         |
| `prettier`    | A code formatter that enforces a consistent style across the codebase. |
| `jest`        | A testing framework for running unit tests and ensuring code quality.  |

<BackToTop />
